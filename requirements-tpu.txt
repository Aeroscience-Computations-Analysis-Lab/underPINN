# underPINN — TPU requirements (Google Colab TPU / Cloud TPU VM)
#
# Usage on Colab (Runtime → Change runtime type → TPU):
#   !pip install -r requirements-tpu.txt
#   !pip install -e . --no-deps          # install underPINN itself (from repo root)
#
# Verify the TPU is visible:
#   import jax; print(jax.devices())     # → [TpuDevice(id=0), ... x8]
#
# Notes
# -----
# * jax[tpu] pulls in the matching libtpu via the extra index below — do NOT
#   also install jax[cpu] or jax[cuda*] in the same environment.
# * Install underPINN with --no-deps afterwards so pip cannot override the
#   TPU jax with the cpu pin from pyproject.toml.

-f https://storage.googleapis.com/jax-releases/libtpu_releases.html
jax[tpu]>=0.4.26

# Core stack (same floors as pyproject.toml)
flax>=0.8.0
optax>=0.2.0
pyyaml>=6.0
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
shapely>=2.0
trimesh>=4.0
pandas>=2.0
